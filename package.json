{
  "name": "crypto-lab-paillier-gate",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "deploy": "npm run build && gh-pages -d dist",
    "preview": "vite preview",
    "verify": "tsx scripts/verify.ts"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
